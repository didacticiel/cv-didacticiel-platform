# backend/apps/cv_app/urls.py

from django.urls import path

# Définition du namespace pour les reverses (recommandé pour Django)
app_name = 'cv_app' 

# Liste des chemins d'URL. Elle doit exister, même vide.
urlpatterns = [
    # Nous ajouterons ici les chemins pour le CRUD du CV plus tard
]